const refreshPage = (innerHTML) => {
  const newPage = document.getElementById('main-display');
  newPage.innerHTML = innerHTML;
};


refreshPage('<%= j render "business_assets/cards_list_map", business_assets: @business_assets, markers: @markers %>');
